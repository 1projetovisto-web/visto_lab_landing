# Firebase Configuration (Copied from firebase-applet-config.json potentially, but here for reference)
# These are usually handled by the dev server via the JSON file directly.

# Resend API Key for sending emails from @visto.art.br
RESEND_API_KEY=re_123456789
